// Where the two browsers pass messages to each other.
//
// Leave this blank and a shared session only works between two tabs of the same
// browser on one machine — enough to try it out, useless for a real participant.
//
// To make it work across the internet, paste your Firebase Realtime Database URL
// below. Setup instructions are in LIVE-SESSION.md, next to this file.
window.LIVE_DATABASE_URL = 'https://cognitive-researcher-test-default-rtdb.firebaseio.com';
